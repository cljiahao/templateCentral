# Shared Skills

Cross-stack skills that are not tied to a specific framework. These can be used by any subagent.

| Skill | Description |
|-------|-------------|
| `task-management/` | Use when the user is working on a complex, multi-step feature and the project's AGENTS.md has task management enabled, or the user explicitly requests structured task management. Not for simple scaffolding or single-file changes. |
| `full-stack-pairing/` | Use when connecting a frontend (Next.js or Vite + React) to a backend (FastAPI or NestJS) — covers proxy config, CORS, environment variables, and cookie forwarding. |
| `remove-example/` | Use when the user wants to remove the example/demo code from a scaffolded project to start with a clean slate. |
