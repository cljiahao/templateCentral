[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
extend-select = ["I"]

[tool.pytest.ini_options]
pythonpath = ["src", "test"]

markers = [
    "unit: unit tests",
    "end_to_end: end to end tests",
]

addopts = [
    "--import-mode=importlib",
]
